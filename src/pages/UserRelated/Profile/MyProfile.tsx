/**
 * REMOVED — safe to delete this file.
 *
 * The self-profile container is superseded by the account overlay's "My Account" and
 * "Quiz Stats" panels (see docs/development/account-overlay.md), which reuse the same
 * `get-user-quiz-stats` hook and `QuizHistoryList` component.
 *
 * `ProfileView` itself is still live — `UserProfile.tsx` renders it for the public
 * profile at `/users/:userId`.
 */
export {};
